/* Asks user to choose 2 x 2 or 3 x 3 array.
 * Creates the array and creates a pointer to the array.
 * Sends array to readMatrix.
 * uses determinant to calculate the determinant.
 * displays array and determinant to user. 
 *
 * dynamically allocate space for array.
 * free the dynamically created memory when done. 
 *
 * Input validation if time. 
